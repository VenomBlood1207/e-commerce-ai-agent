VITE_API_URL=http://localhost:8000

# Copy this file to `.env` and adjust the URL if your backend runs on a different host/port.
# Vite only exposes env vars that start with VITE_ to the client (import.meta.env.VITE_API_URL).
